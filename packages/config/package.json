{
  "name": "@ryvra/config",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "lint": "eslint --max-warnings 0 --ext .ts src",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "build": "tsc -p tsconfig.json",
    "test": "node --experimental-strip-types --test src/__tests__/**/*.spec.mjs"
  },
  "dependencies": {
    "zod": "^3.24.1"
  }
}
